name: fidiko_flutter
description: Application Mobile & Web Flutter FIDIKO pour le vote local et à distance.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  socket_io_client: ^2.0.3+1
  provider: ^6.1.2
  vibration: ^1.9.0
  flutter_spinkit: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
